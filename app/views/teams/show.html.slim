h1= @team.name
p= "Rank: #{@team.rank}"
p= "Owner: #{@team.user.name}"
p= "Salary Cap: #{number_to_currency(@team.salary_cap, precision: 2)}"
p= "Salary Remaining: #{number_to_currency(@team.remaining_salary, precision: 2)}"
